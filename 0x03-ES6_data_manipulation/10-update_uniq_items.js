export default function updateUniqueItems(map) {
  if (!(map instanceof Map)) throw Error('Cannot process');
  for (const prouduct of map) if (product[1] === 1) map.set(product[0], 100);
  return map;
}
